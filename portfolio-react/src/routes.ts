/** Central route paths — use in links and tests to avoid string drift */
export const ROUTES = {
  root: '/',
  editorial: '/editorial',
  workBoss: '/experience',
  projectsBoss: '/projects',
  contact: '/contact',
  about: '/about',
  blogs: '/blogs',
} as const;
