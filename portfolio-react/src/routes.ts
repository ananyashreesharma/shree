/** Central route paths — use in links and tests to avoid string drift */
export const ROUTES = {
  root: '/',
  editorial: '/editorial',
  workBoss: '/work-boss',
  projectsBoss: '/projects-boss',
} as const;
